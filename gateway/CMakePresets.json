{
    "version": 8,
    "configurePresets": [
        {
            "name": "default",
            "displayName": "Default (Kit-based)",
            "description": "Default configuration using CMake Tools Kit selector",
            "binaryDir": "${sourceDir}/out/build/${presetName}",
            "cacheVariables": {
                "CMAKE_INSTALL_PREFIX": "${sourceDir}/out/install/${presetName}",
                "CMAKE_BUILD_TYPE": "Debug"
            }
        }
    ]
}
services:
  auth-service:
    image: ghcr.io/${REGISTRY_OWNER}/securecloud-auth-service:${IMAGE_TAG}
    pull_policy: always
    build: null

  gateway:
    image: ghcr.io/${REGISTRY_OWNER}/securecloud-gateway:${IMAGE_TAG}
    pull_policy: always
    build: null

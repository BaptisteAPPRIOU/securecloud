# =============================================================================
# SecureCloud - Staging/CI Environment Template
# =============================================================================
# Copy this file to config/env/staging/.env for real staging deployments and
# replace all placeholder secrets. This tracked template is also used by CI as
# an explicit compose env-file to avoid implicit defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
DB_NAME=securecloud
DB_USER=scuser
DB_PASS=scpassword

# -----------------------------------------------------------------------------
# REDIS
# -----------------------------------------------------------------------------
REDIS_PASSWORD=redispass

# -----------------------------------------------------------------------------
# JWT
# -----------------------------------------------------------------------------
JWT_SECRET=change-me-in-production-32-chars
JWT_ISSUER=securecloud-auth
JWT_AUDIENCE=securecloud-client
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=604800

# -----------------------------------------------------------------------------
# GATEWAY / TLS
# -----------------------------------------------------------------------------
ENABLE_TLS=true
GATEWAY_HTTP_PORT=8080
GATEWAY_HTTPS_PORT=8443
GATEWAY_TLS_PORT=8443
GATEWAY_CERT_FILE=/etc/securecloud/certs/tls.crt
GATEWAY_KEY_FILE=/etc/securecloud/certs/tls.key
GATEWAY_CERTS_DIR=./ops/certs/gateway

# -----------------------------------------------------------------------------
# CLIENT
# -----------------------------------------------------------------------------
SECURECLOUD_API_BASE=https://gateway:8443
SECURECLOUD_DEV_ALLOW_SELF_SIGNED=true
SECURECLOUD_TLS_CA_FILE=/app/certs/ca.crt
QT_QPA_PLATFORM=offscreen

# -----------------------------------------------------------------------------
# OBSERVABILITY / ADMIN
# -----------------------------------------------------------------------------
LOG_LEVEL=info
ADMINER_PORT=9090
PROMETHEUS_PORT=9091
GRAFANA_PORT=3000
GRAFANA_PASSWORD=admin

# -----------------------------------------------------------------------------
# DEPLOY OVERLAY (compose.deploy.yml)
# -----------------------------------------------------------------------------
REGISTRY_OWNER=securecloud
IMAGE_TAG=latest
